import React from 'react';

const Movies = () => {
  return (
    <div>
      <h1>Llamada exitosa a la API de películas autenticada (React + Axios + JWT)</h1>
    </div>
  );
};

export default Movies;